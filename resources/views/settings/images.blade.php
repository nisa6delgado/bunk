<div style="display: flex; justify-content: space-between">
    <div>
        @if($favicon)
            <img src="/img/{{ $favicon }}">
        @endif
    </div>

    <div>
        @if($brand)
            <img src="/img/{{ $brand }}">
        @endif
    </div>
</div>